import { JwtInterceptorService } from './jwt-interceptor.service';

describe('JwtInterceptor', () => {
  it('should create an instance', () => {
    expect(new JwtInterceptorService()).toBeTruthy();
  });
});
