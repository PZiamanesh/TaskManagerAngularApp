import { ClienLocationModel } from './clien-location-model';

describe('ClienLocationModel', () => {
  it('should create an instance', () => {
    expect(new ClienLocationModel()).toBeTruthy();
  });
});
